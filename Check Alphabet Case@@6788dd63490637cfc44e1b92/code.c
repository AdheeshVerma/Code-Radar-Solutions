#include <stdio.h>

int main() {
    int c;
    scanf("%d",&c);
    if(90>=c>=65){
        printf("Uppercase");
    }
    else{
        printf("Lowercase");
    }
   // printf("%s", welcome());
    return 0;
}